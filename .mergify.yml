pull_request_rules:
- name: automatic merge pull request from atom-im
  conditions:
    - author=atom-im
  actions:
    merge:
      method: squash
- name: automatic merge pull request from huwan
  conditions:
    - author=huwan
  actions:
    merge:
      method: squash
