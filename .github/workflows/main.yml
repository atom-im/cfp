name: Github Pages

on:
  push:
    branches:
      - master
  schedule:
    - cron: '1 */1 * * *'
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-18.04
    steps:
      - name: Setup timezone
        uses: zcong1993/setup-timezone@v1.1.1
        with:
          timezone: Asia/Hong_Kong

      - uses: actions/checkout@v2

      - name: Setup Hugo
        uses: peaceiris/actions-hugo@v2
        with:
          hugo-version: '0.68.3'

      - name: Build
        run: hugo

      - name: Check Changes
        working-directory: ./public
        run: |
          changes="$(git diff -U0|grep @@|wc -l)"
          updated="$(git diff|grep updated)"
          echo "LOG: $changes"
          echo "LOG: $updated"
          if [ "$changes" = "1" ] && [ ! -z "$updated" ]; then
            git checkout .
          fi

      - name: Deploy
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./public
          disable_nojekyll: true
          full_commit_message: ${{ github.event.head_commit.message }}
