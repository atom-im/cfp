if (!window.jQuery) {
    throw ("jquery.dblp.js: jQuery library has not been loaded.")
} (function(a) {
    a.QueryString = (function(c) {
        if (c == "") {
            return {}
        }
        var b = {};
        for (var e = 0; e < c.length; ++e) {
            var d = c[e].indexOf("=");
            if (d == -1) {
                parts = [c[e], ""]
            } else {
                parts = [c[e].slice(0, d), c[e].slice(d + 1)]
            }
            b[parts[0]] = decodeURIComponent(parts[1].replace(/\+/g, " "))
        }
        return b
    })(window.location.search.substr(1).split("&"))
})(jQuery); (function(a) {
    a.fn.invisible = function() {
        return a(this).css("visibility", "hidden")
    };
    a.fn.visible = function() {
        return a(this).css("visibility", "visible")
    };
    a.fn.visibilityToggle = function() {
        return a(this).css("visibility",
        function(c, b) {
            return (b == "visible") ? "hidden": "visible"
        })
    }
} (jQuery)); (function(a) {
    a.fn.selectText = function() {
        var e = document,
        c = this[0],
        b,
        d;
        if (e.body.createTextRange) {
            b = document.body.createTextRange();
            b.moveToElementText(c);
            b.select()
        } else {
            if (window.getSelection) {
                d = window.getSelection();
                b = document.createRange();
                b.selectNodeContents(c);
                d.removeAllRanges();
                d.addRange(b)
            }
        }
        return a(this)
    }
} (jQuery)); (function(a) {
    a.fn.getSelectionText = function() {
        var b = "";
        if (window.getSelection) {
            b = window.getSelection().toString()
        } else {
            if (document.selection && document.selection.type != "Control") {
                b = document.selection.createRange().text
            }
        }
        return b
    }
} (jQuery));